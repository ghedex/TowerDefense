package MainRef.Configuration;

public abstract class LevelConfiguration {
/*TODO write a configuration with which each level can be configured
* Check which Objects a level needs an how they have to be able to be configured
* */
}
